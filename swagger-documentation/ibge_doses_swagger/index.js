const ibge_doses_definition = require('./ibge_doses_definition.json');
const ibge_doses_paths = require('./ibge_doses_paths.json');

module.exports = {
    definition: ibge_doses_definition, 
    paths: ibge_doses_paths,
};
